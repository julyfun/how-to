---
title: "285 hw3"
date: 2026-06-23 21:18:39
tags: ["技术学习", "285"]
author: "julyfun.m5air"
os: "Darwin julyfundeMacBook-Air.local 25.3.0 Darwin Kernel Version 25.3.0: Wed Jan 28 20:56:42 PST 2026; root:xnu-12377.91.3~2/RELEASE_ARM64_T8142 arm64"
assume-you-know: [computer]
confidence: 2
---

## 2.4: CartPole DQN

### 回忆提纲
See [Lec8 伪代码](./285-2.md). 仅支持离散空间，因为没有 actor 我们必须选 action s.t. Q(s, a) 最大，在离散空间，Q_net 直接输出一个数组包含所有 a 的价值即可。

### 回答问题
• Submit your logs of CartPole-v1, and a plot with environment steps on the x-axis and eval return on the y-axis.
![](https://how-to-1258460161.cos.ap-shanghai.myqcloud.com/how-to/20260625204520715.png)

其中 target values 就是 `r + gamma * Q(s_next, a_next)` 估计，即 `Q(s, a)` 的拟合目标.

### 复现
```bash
conda install -c conda-forge swig
# 但是我们并不使用 conda，所以把 conda base bin 加入 PATH.

uv run src/scripts/run_dqn.py -cfg experiments/dqn/cartpole.yaml --eval_interval 2500
```

## 2.5: Cartpole Double-DQN

### 回忆提纲
确实只有一行不同，和 Lec8 伪代码一致。

### 回答问题
• Run double-Q DQN on LunarLander-v2:
You should expect a return of 200 at least once during training. Plot the eval return over training steps.

![](https://how-to-1258460161.cos.ap-shanghai.myqcloud.com/how-to/20260625221944485.png)

能看出来确实成功了。

• Run your DQN implementation on the MsPacman-v0 problem. Our default configuration will use double-
Q learning by default. You are welcome to tune hyperparameters to get it to work better, but the
default parameters should work (so if they don’t, you likely have a bug in your implementation). Your
implementation should receive a score of around 1500 at least once during training.

![](https://how-to-1258460161.cos.ap-shanghai.myqcloud.com/how-to/20260626145739246.png)

### 复现
```bash
uv run src/scripts/run_dqn.py -cfg experiments/dqn/lunarlander.yaml
```

## 3.2: Soft Actor-Critic 的 critic

### 回忆提纲
本节仅实现 SAC 的 critic 部分，任务全部改为连续空间. SAC 的 actor 输出是 mean & logstd 采样. update() 使用的 next_action 不再需要像 DQN 那样让 critic 来选，而是 policy 直接输出并 sample.

```python
s, a, r, nxt_s, done = replaybuffer.sample()
mseloss(
    online_critic(s, a),
    (no_grad) r + gamma * target_critic(nxt_s, policy(nxt_s).sample()) * (1 - done)
    #                                          |<--------->|
    #                                       this is a distribution
)
```

### 回答问题
其实此图用了错的代码. 先不更新了.
![](https://how-to-1258460161.cos.ap-shanghai.myqcloud.com/how-to/20260626192205816.png)


## 附录
**如何使用其他目录的环境运行当前目录：**
```python
cd hw3
source ../hw2/.venv/bin/activate.fish
PYTHONPATH=src uv run --active \
    src/scripts/run_dqn.py  -cfg experiments/dqn/lunarlander.yaml
```
