#include <bits/stdc++.h>
using namespace std;
const int N = 1e2 + 10;
int a[N];
int main() {
    int t; cin >> t;
    while (t--) {
        int n, k; 
        cin >> n >> k;
        for (int i = 1; i <= n; i++)
            cin >> a[i];
    }
    return 0;
}